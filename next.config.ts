import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Instrumentation is enabled by default in Next.js 16+
  // The instrumentation.ts file in src/ will run on startup
};

export default nextConfig;
